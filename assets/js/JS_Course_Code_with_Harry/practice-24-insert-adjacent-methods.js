let first = document.getElementById("first");

// first.insertAdjacentHTML('beforebegin', '<div class="beforebegin">beforebegin</div>')
// first.insertAdjacentHTML('beforeend', '<div class="beforeend">beforeend</div>')
// first.insertAdjacentHTML('afterbegin', '<div class="afterbegin">afterbegin</div>')
// first.insertAdjacentHTML('afterend', '<div class="afterend">afterend</div>')


first.remove();